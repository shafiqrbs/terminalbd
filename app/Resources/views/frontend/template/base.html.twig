<!DOCTYPE html>
<html lang="en">
<!--[if IE 8]> <html lang="en" class="ie8 no-js"> <![endif]-->
<!--[if IE 9]> <html lang="en" class="ie9 no-js"> <![endif]-->
<!--[if !IE]><!--> <html lang="en" class="no-js"> <!--<![endif]-->
<head>
    {% set currentPath = path(app.request.attributes.get('_route'),
    app.request.attributes.get('_route_params')) %}

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="user-scalable=no, initial-scale=1.0, maximum-scale=1.0"/>
    <meta name="apple-mobile-web-app-capable" content="yes"/>
    <meta name="apple-mobile-web-app-status-bar-style" content="black">
   {# <meta property="og:url"           content="htpp://www.{{ globalOption.domain }}{{ currentPath }}" />
    <meta property="og:type"          content="website" />
    <meta property="og:title"         content="{{ globalOption.name }}" />
    <meta property="og:description"   content="{{ globalOption.name }}" />
    <meta property="og:image"         content="{{ image(globalOption.templateCustomize.webPath('logo')).resize() }}" />
#}

    <meta property="og:title" content="Join the best company in the world!" />
    <meta property="og:url" content="http://www.sharethis.com" />
    <meta property="og:image" content="http://sharethis.com/images/logo.jpg" />
    <meta property="og:description" content="ShareThis is its people. It's imperative that we hire smart,innovative people who can work intelligently as we continue to disrupt the very category we created. Come join us!" />
    <meta property="og:site_name" content="ShareThis" />

    <title>{{ globalOption.name }}</title>
    <link rel="shortcut icon" href="{{ asset('assets/img/favicon.ico') }}">
    <script src="{{ asset('bundles/fosjsrouting/js/router.js') }}"></script>
    <script src="{{ path('fos_js_routing_js', {"callback": "fos.Router.setData"}) }}"></script>
    <!-- Google fonts - witch you want to use - (rest you can just remove) -->
    <link rel='stylesheet' href='http://fonts.googleapis.com/css?family=Open+Sans:400,800,700italic,700,600italic,600,400italic,300italic,300|Roboto:100,300,400,500,700&amp;subset=latin,latin-ext' type='text/css' />
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <!-- HEADER -->
    <!-- HEADER -->
    {% set template = globalOption.siteSetting.theme.folderName|lower %}
    {#{% set csstemplate = "assets/template/desktop/{{ template }}/css/style.css" %}#}
    {% stylesheets filter='cssrewrite'
    'assets/template/desktop/css/bootstrap.min.css'
    'assets/template/desktop/font-awesome/css/font-awesome.min.css'
    'assets/template/plugin/owl.carousel/owl.carousel.min.css'
    'assets/template/plugin/owl.carousel/owl.theme.min.css'
    'assets/template/plugin/side.toolbar/vcore.sidebar.css'
    'assets/template/plugin/pgslider/pgwslider.min.css'
    'assets/template/plugin/revolution_slider/css/rs-style.css'
    'assets/template/plugin/revolution_slider/rs-plugin/css/settings.css'
    'assets/template/plugin/textillate/assets/animate.css'
    'assets/template/plugin/wow/css/animate.css'
    'assets/template/plugin/select2/select2.css'
    'assets/template/plugin/range-slider/bootstrap-slider.css'
    'assets/template/plugin/ladda/ladda-themeless.min.css'
    'assets/template/desktop/css/module.css'
    %}

    <link rel="stylesheet" href="{{ asset_url }}"/>
    {% endstylesheets %}
      <link href="/assets/template/desktop/{{ template }}/css/style.css" rel="stylesheet" />

    <!--[if lt IE 9]>

    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
    {% block cssBlock %}{% endblock %}
</head>
<body data-spy="scroll">
{{ include('frontend/template/templateCustomize.html.twig')}}
<!-- Navigation -->
{% render controller("FrontendBundle:TemplateWidget:header",{'globalOption':globalOption.id, 'pageName':pageName}) %}
<div class="clearfix"></div>
{% block body %}{% endblock %}
{#<section class="footer-menu">
    {% if globalOption.featureWidgets is defined and globalOption.featureWidgets is not empty %}
        <section class="feature-widget-footer">
            {% render controller("FrontendBundle:EcommerceWidget:featureWidget",{'globalOption':globalOption.id,'pageName':pageName,'position':'footer'}) %}
        </section>
    {% endif %}
    <div class="container social-payment" >
        <div class="row">
            <div class="col-sm-5">
                <div class="bs-callout" >
                    <h4>PAYMENT METHOD</h4>
                    <p><img src="{{ asset('assets/img/credit-card.png') }}" ></p>
                </div>
            </div>
            <div class="col-sm-4">
                <h4>NEWS LETTER</h4>
                <div class="social-tooltip">
                    <div class="input-group"> <input class="form-control" placeholder="Enter your e-mail" aria-label="Text input with multiple buttons"> <div class="input-group-btn"> <button type="button" class="btn blue btn-default" aria-label="Help"><span data-toggle="tooltip" title="Are you interest new product & offer to send please your e-mail." class="glyphicon glyphicon-question-sign"></span></button> <button type="button" class="btn btn-default green"><span class="glyphicon glyphicon-saved"></span></button> </div> </div> </div>

            </div>
            <div class="col-sm-3">
                <h4>KEEP IN TOUCH</h4>
                <div class="social-tooltip">
                    <a data-toggle="tooltip" id="facebook" title="Follow on Facebook" target="_blank" href="{{ globalOption.facebookPageUrl }}"><i class="fa fa-facebook fa-fw  "></i></a>
                    <a data-toggle="tooltip" id="twitter" title="Follow on Twitter" target="_blank" href="{{ globalOption.instagramPageUrl }}"><i class="fa fa-twitter fa-fw  "></i></a>
                    <a data-toggle="tooltip" id="instagram" title="Follow on Instagram" target="_blank" href="{{ globalOption.twitterUrl }}"><i class="fa fa-instagram fa-fw  "></i></a>
                    <a data-toggle="tooltip" id="email" title="Send as an email" target="_blank"  href="mailto:{{ globalOption.email }}"><i class="fa fa-envelope fa-fw  "></i></a>
                </div>
            </div>
        </div>

    </div>
    <div class="container" id="footer-menu" >
        <div class="row">
            <div class="col-sm-4">
                <div class="bs-callout" >
                    <h4>WHO WE ARE</h4>
                    <p>
                     {% render controller("FrontendBundle:TemplateWidget:about",{'globalOption':globalOption.id,'wordlimit':300}) %}
                    </p>
                </div>

            </div>
            <div class="col-sm-3">
                <h4>{{ globalOption.name }}</h4>
                {% render controller("FrontendBundle:TemplateWidget:footerMenu",{'globalOption':globalOption.id,'menuGroup':3}) %}
            </div>
            <div class="col-sm-2">
                <h4>HELP</h4>
                {% render controller("FrontendBundle:TemplateWidget:footerMenu",{'globalOption':globalOption.id,'menuGroup':7}) %}
            </div>
            <div class="col-sm-3">
                <h4>SHOP/OFFICE ADDRESS</h4>
                <address>
                    {% if globalOption.contactPage.address1 is not empty %}

                                 <p style="font-weight: bolder"> <i class="fa fa-home"></i>&nbsp;{{ globalOption.contactPage.address1 }}<br>
                        {% if globalOption.contactPage.location is defined and globalOption.contactPage.location %}
                            {{ globalOption.contactPage.location.name }}, {{ globalOption.contactPage.location.parent.name }}
                        {% endif %}
                        {% if globalOption.contactPage.postalCode is defined and globalOption.contactPage.postalCode %}
                            -{{ globalOption.contactPage.postalCode }}
                        {% endif %}
                                 </p>
                    {% endif %}
                    {% if globalOption.contactPage.startHour %}
                        <i class="fa fa-clock-o"></i>&nbsp;<strong>{{ globalOption.contactPage.startHour }} To {{ globalOption.contactPage.endHour }}</strong>
                    {% endif %}
                    {% if globalOption.contactPage.weeklyOffDay is not empty %}
                        <br><i class="fa fa-calendar-o" aria-hidden="true"></i>&nbsp;<strong>
                                    {% for day in globalOption.contactPage.weeklyOffDay %}
                                        {{ day }}
                                        {% if not loop.last %},{% endif %}
                                    {% endfor %}
                                </strong>
                    {% endif %}
                    <br>
                                <i class="fa fa-mobile-phone"></i>&nbsp;+ 088&nbsp;<a href="tel:+ 088 {{ globalOption.mobile }}" class="contact-call">{{ globalOption.mobile }}</a>
                                <i class="fa fa-map-pin"></i>&nbsp;<a target="_blank" href="https://maps.google.com?saddr=Current+Location&daddr=23.801422,90.352599">Direction</a>

                </address>
            </div>
        </div>
    </div>
</section>#}
<footer id="footer">
     <div class="container">
        <div class="row myfooter">
            <div class="col-sm-6">
                <div class="pull-left">
                    &copy; Copyright {{ globalOption.name }} {{ "now"|date('Y') }}
                </div>
            </div>
            <div class="col-sm-6">
                <div class="pull-right">Designed & Developed by <a target="_blank" href="http://www.terminalbd.com">{{ portal_design_development }}</a></div>
            </div>
        </div>
    </div>
</footer>
<div id="myModal" class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" >
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content"></div>
    </div>
</div>
{% render controller("FrontendBundle:TemplateWidget:modalLogin",{'globalOption':globalOption.id}) %}
{% if globalOption.templateCustomize.showSidebar == 1 %}
    <div class="side-toolbar" style=""></div>
{% endif %}
<!-- jQuery -->
<!--Jquery Smooth Scrolling-->
{% javascripts

'assets/template/desktop/js/jquery.js'
'assets/template/desktop/js/bootstrap.min.js'
'assets/template/plugin/jquery-inputmask/jquery.inputmask.bundle.min.js'
'assets/template/plugin/numeric/jquery.numeric.js'
'assets/template/plugin/validation/jquery.validate-1.14.0.min.js'
'assets/template/plugin/validation/jquery-validate.bootstrap-tooltip.min.js'
'assets/template/plugin/owl.carousel/owl.carousel.min.js'
'assets/template/plugin/side.toolbar/vcore.sidebar.js'
'assets/template/plugin/pgslider/imagezoom.js'
'assets/template/plugin/pgslider/pgwslider.js'
'assets/template/plugin/revolution_slider/rs-plugin/js/jquery.themepunch.plugins.min.js'
'assets/template/plugin/revolution_slider/rs-plugin/js/jquery.themepunch.revolution.min.js'
'assets/template/plugin/textillate/assets/jquery.fittext.js'
'assets/template/plugin/textillate/assets/jquery.lettering.js'
'assets/template/plugin/matchHeight/height-match.js'
'assets/template/plugin/select2/select2.js'
'assets/template/plugin/range-slider/bootstrap-slider.min.js'
'assets/template/plugin/ladda/spin.min.js'
'assets/template/plugin/ladda/ladda.min.js'
'assets/template/plugin/treeview/treeview.js'
'assets/template/plugin/jquery-zoom/zoom.js'
'assets/template/plugin/wow/js/wow.min.js'
%}
<script type="text/javascript" src="{{ asset_url }}"></script>
{% endjavascripts %}

<script>  subdomain = "{{ globalOption.subDomain }}"; </script>
{% block javascriptBlock %}{% endblock %}
<script>
    wow = new WOW(
        {
            boxClass:     'wow',
            animateClass: 'animated',
            offset:       100
        }
    );
    wow.init();
    $(document).ready(function(){

         var navbar = $('.navbar-header').outerHeight(true);
         var mins = 22;
         $('.bs-example-form').css({
             'margin-top':(navbar/2 - mins)
         });

        var stickyOffset = $('.sticky').offset().top;

         $(window).scroll(function(){
             var sticky = $('.sticky'),
                 scroll = $(window).scrollTop();
             if (scroll >= stickyOffset) sticky.addClass('fixed');
             else sticky.removeClass('fixed');
         });

        $('.social-tooltip').tooltip({
             selector: "[data-toggle=tooltip]",
             container: "body"
         });

        $('.select2').select2({
            placeholder: "Filter by category",
            allowClear: true,
            color: "black"
        });

        $(".numeric").numeric();
        $(".mobile").inputmask("mask", {"mask": "99999-999-999"}); //specifying fn & options
        $('#searchEvent').click(function(){
            $('#nav-search').slideToggle('slow');
        });

        $('#catlist').children('.news-list').each(function(index) {
            $(this).addClass(index % 2 ? 'odd' : 'even');
        });

        $('.side-toolbar').vSidebar({
            'items': [
                {% render controller("FrontendBundle:TemplateWidget:sidebar",{'globalOption':globalOption.id}) %}
            ],
            'position': 'right bottom'
        });

         $(".dropdown").hover(
                 function() {
                     $('.dropdown-menu', this).not('.in .dropdown-menu').stop(true,true).slideDown("400");
                     $(this).toggleClass('open');
                 },
                 function() {
                     $('.dropdown-menu', this).not('.in .dropdown-menu').stop(true,true).slideUp("400");
                     $(this).toggleClass('open');
                 }
         );

        var owlProduct = $("#preview-slider");
        owlProduct.owlCarousel({
            items:3,
            itemsDesktop:[1199,3],
            itemsDesktopSmall:[979,2],
            itemsTablet:[768,1],
            pagination: false,
            paginationNumbers: false,
            autoPlay:false,
            rewindNav:false
        });
        // Custom Navigation Events
        $(".preview-next").click(function(){
            owlProduct.trigger('owl.next');
        })

        $(".preview-prev").click(function(){
            owlProduct.trigger('owl.prev');
        })

        var owlClient = $("#client-slider");
        owlClient.owlCarousel({
            items:5,
            itemsDesktop:[1199,5],
            itemsDesktopSmall:[979,3],
            itemsTablet:[768,1],
            pagination: false,
            paginationNumbers: false,
            autoPlay:true,
            rewindNav:false
        });

        // Custom Navigation Events
        $(".next-client").click(function(){
            owlClient.trigger('owl.next');
        })

        $(".prev-client").click(function(){
            owlClient.trigger('owl.prev');
        })

        $("#testimonial-single-slider").owlCarousel({
            items:1,
            itemsDesktop:[1000,1],
            itemsDesktopSmall:[979,1],
            itemsTablet:[768,1],
            pagination: true,
            autoPlay:false
        });

        $("#news-slider").owlCarousel({
            items :3,
            itemsDesktop:[1000,1],
            itemsDesktopSmall:[980,1],
            navigation : false,
            pagination:false,
            autoPlay : true
        });


        $('#list').click(function(event){event.preventDefault();$('#product .item').addClass('list-group-item');});
        $('#grid').click(function(event){event.preventDefault();$('#product .item').removeClass('list-group-item');$('#products .item').addClass('grid-group-item');});

        $('.custom-menu a[href^="#"], .intro-scroller .inner-link').on('click',function (e) {

            e.preventDefault();
            var target = this.hash;
            var $target = $(target);

            $('html, body').stop().animate({
                'scrollTop': $target.offset().top
            }, 900, 'swing', function () {
                window.location.hash = target;
            });
        });

        $('a.page-scroll').bind('click', function(event) {
            var $anchor = $(this);
            $('html, body').stop().animate({
                scrollTop: $($anchor.attr('href')).offset().top
            }, 1500, 'easeInOutExpo');
            event.preventDefault();
        });

        $(".nav a").on("click", function(){
            $(".nav").find(".active").removeClass("active");
            $(this).parent().addClass("active");
        });

        $('body').append('<div id="toTop" class="btn btn-primary color1"><span class="glyphicon glyphicon-chevron-up"></span></div>');
        $(window).scroll(function () {
            if ($(this).scrollTop() != 0) {
                $('#toTop').fadeIn();
            } else {
                $('#toTop').fadeOut();
            }
        });
        $('#toTop').click(function(){
            $("html, body").animate({ scrollTop: 0 }, 700);
            return false;
        });

        $("#contactForm").validate({

            rules: {
                name: {required: true},
                mobile: {required: true}
            },
            messages: {
                name: "Just check the box<h5 class='text-danger'>You aren't going to read the EULA</h5>",
                mobile: "Enter valid mobile no"
            },
            tooltip_options: {
                name: {trigger:'focus'},
                mobile: {placement:'top',html:true}
            },
        });

         function heightsEqualizer(selector) {
             var elements = document.querySelectorAll(selector),
                     max_height = 0,
                     len = 0,
                     i;

             if ( (elements) && (elements.length > 0) ) {
                 len = elements.length;
                 for (i = 0; i < len; i++) { // get max height
                     elements[i].style.height = ''; // reset height attr
                     if (elements[i].clientHeight > max_height) {
                         max_height = elements[i].clientHeight;
                     }
                 }

                 for (i = 0; i < len; i++) { // set max height to all elements
                     elements[i].style.height = max_height + 'px';
                 }
             }
         }

         if (document.addEventListener) {
             document.addEventListener('DOMContentLoaded', function() {
                 heightsEqualizer('.js-equal-height');
             });
             window.addEventListener('resize', function(){
                 heightsEqualizer('.js-equal-height');
             });
         }

         setTimeout(function () { // set 1 second timeout for having all fonts loaded
             heightsEqualizer('.js-equal-height');
         }, 1000);


         if (document.addEventListener) {
             document.addEventListener('DOMContentLoaded', function() {
                 heightsEqualizer('.product-equal-height');
             });
             window.addEventListener('resize', function(){
                 heightsEqualizer('.product-equal-height');
             });
         }

         setTimeout(function () { // set 1 second timeout for having all fonts loaded
             heightsEqualizer('.product-equal-height');
         }, 1000);


     {% block documentready %}{% endblock %}

    });
    function gallery(){};

    var $itemsHolder = $('ul.port2');
    var $itemsClone = $itemsHolder.clone();
    var $filterClass = "";
    $('ul.filter li').click(function(e) {
        e.preventDefault();
        $filterClass = $(this).attr('data-value');
        if($filterClass == 'all'){ var $filters = $itemsClone.find('li'); }
        else { var $filters = $itemsClone.find('li[data-type='+ $filterClass +']'); }
        $itemsHolder.quicksand(
                $filters,
                { duration: 1000 },
                gallery
        );
    });
    $(document).ready(gallery);

</script>
<style>
    .tooltip{
        z-index: 1000!important;
    }
    .form-control, .form-control:focus{
        border-color: #cccccc;
        -webkit-box-shadow: none;
        box-shadow: none;
        background: #fff;
    }

    textarea:focus,
    input[type="text"]:focus,
    input[type="password"]:focus,
    input[type="datetime"]:focus,
    input[type="datetime-local"]:focus,
    input[type="date"]:focus,
    input[type="month"]:focus,
    input[type="time"]:focus,
    input[type="week"]:focus,
    input[type="number"]:focus,
    input[type="email"]:focus,
    input[type="url"]:focus,
    input[type="search"]:focus,
    input[type="tel"]:focus,
    input[type="color"]:focus,
    .uneditable-input:focus {
        border-color: #285e8e;
        box-shadow:none;
        outline: 0 none;
    }
</style>
</body>
</html>
