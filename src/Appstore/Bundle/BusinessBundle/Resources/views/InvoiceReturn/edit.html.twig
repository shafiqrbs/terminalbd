
{% extends '::base.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <!-- BEGIN PAGE HEADER-->
        <div class="row-fluid">
            <div class="span12">
                <h3 class="page-title">
                    Customer Name : {{ entity.customer.name }},{{ entity.customer.mobile }}
                </h3>
                <ul class="breadcrumb">
                    <li>
                        <i class="icon-home"></i>
                        <a href="{{ path('homepage') }}">Home</a>
                        <span class="icon-angle-right"></span>
                    </li>
                    <li><a href="{{ path('business_invoice_return') }}">Manage Invoice Return</a></li>
                    <li class="pull-right btn blue disabled mini">
                        <i class="fa fa-calendar"></i>
                        {{ "now"|date("d-F-Y") }}
                    </li>
                </ul>
            </div>
        </div>
        <!-- END PAGE HEADER-->
        <!-- BEGIN PAGE CONTENT-->
        <div class="row-fluid">
            <div class="span12">
                <div class="portlet box green">

                    <div class="portlet-title">
                        <div class="caption"><i class="icon-reorder"></i>Product Invoice Return Information</div>
                        <div class="actions">
                            <div class="btn-group">
                                <a class="btn" href="{{ path('business_invoice_return') }}"><i class="icon-th-list"></i> Purchase Return</a>
                            </div>
                        </div>
                    </div>

                    <div class="portlet-body">
                        <div class="clearfix"><br/></div>
                        <div class="portlet-body form">
                            {% include('flash-message.html.twig')  %}
                            <!-- BEGIN FORM-->
                            <form class="" id="purchaseReturn" name="purchaseReturn" method="post" action="{{ path('business_invoice_return_update',{'id':entity.id}) }}"  >
                                {% if particulars is not empty %}
                                <table class="table table-bordered table-striped table-condensed flip-content ">
                                    <thead class="flip-content ">
                                    <tr class="head-blue">
                                        <th class="numeric span6" >Product Name </th>
                                        <th class="numeric span1" >Price</th>
                                        <th class="numeric span1" >Current QTY</th>
                                        <th class="numeric span1" >Return QTY</th>
                                        <th class="numeric span1" >SubTotal</th>
                                        <th class="numeric span1" >Mode</th>

                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for particular in particulars %}
                                        <tr>
                                            <th class="numeric" >{{ loop.index }}. {{ particular.itemName }}
                                                <input type="hidden" name="subTotal[]" id="sub-{{  particular.itemId }}" class="subTotal" value="0">
                                                <input type="hidden" name="itemId[]" value="{{ particular.itemId }}">
                                                <input type="hidden" name="price[]" value="{{ particular.salesPrice }}">
                                            </th>
                                            <td class="numeric" > {{ particular.salesPrice }}</td>
                                            <td class="numeric" > {{ particular.quantity }}</td>
                                            <td class="numeric" ><input class="m-wrap span12 form-control input-number" type="text" name="quantity[]" data-id="{{ particular.itemId }}" data-value="{{ particular.salesPrice }}" id="quantity-{{  particular.itemId }}" value="" placeholder="Input quantity"></td>
                                            <td class="numeric" id="subTotal-{{  particular.itemId }}" ></td>
                                            <td class="numeric">
                                                <select class="span12 m-wrap input" name="itemProcess" id="itemProcess">
                                                    {% set itemProcesses = ['Stock-Return','Purchase-Return','Damage'] %}
                                                    {% for itemProcesse in itemProcesses %}
                                                        <option value="{{ itemProcesse }}">{{ itemProcesse }}</option>
                                                    {% endfor %}
                                                </select>
                                            </td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                    <tfoot>
                                    <tr><th colspan="4" class="text-right"><span class="text-right">Total </span></th><th id="total"></th>
                                        <td></td>
                                    </tr>
                                    <tr><th colspan="4" ><span class="text-right">Previous Due</span></th><th>{{ balance|number_format(2) }}</th>
                                    <td></td>
                                    </tr>
                                    <tr><th colspan="4" class="text-right"><span class="text-right">Process Amount</span></th>
                                    <th>
                                        <input class="m-wrap span12 form-control input-number" type="text" name="payment" id="payment" value="" placeholder="Payment">
                                    </th>
                                    <td>
                                        <input class="m-wrap span12 form-control input-number" type="text" name="adjustment" id="adjustment" value="" placeholder="Adjustment">
                                    </td>
                                    </tr>
                                    </tfoot>
                                </table>
                                {% endif %}
                                <div class="clearfix"><br/></div>
                                <!--/span-->
                                <div class="form-actions portlet box grey">
                                    <div class="pull-right">
                                        <input type="hidden" id="grandTotal" name="grandTotal" value="" >
                                        <button type="button" id="submitBtn" class="btn green approve" disabled="disabled"><i class="icon-save"></i> Save</button>
                                    </div>
                                </div>
                            </form>

                            <!-- END FORM-->
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
{% endblock %}
{% block javascriptBlock %}
    {{ parent() }}
    {% javascripts
    '@BusinessBundle/Resources/public/js/invoice-return.js'
    %}
    <script type="text/javascript" src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock  %}
{% block documentready %}
    {{ parent() }}
{% endblock %}


