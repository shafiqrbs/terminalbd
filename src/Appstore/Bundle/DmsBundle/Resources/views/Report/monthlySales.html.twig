{% extends '::base.html.twig' %}
{% block body %}
<div class="container-fluid">
    <!-- BEGIN PAGE HEADER-->
    <div class="row-fluid">
        <div class="span12">
            <h3 class="page-title">
                Sales overview
            </h3>
            <ul class="breadcrumb">
                <li>
                    <i class="icon-home"></i>
                    <a href="{{ path('homepage') }}">Home</a>
                    <span class="icon-angle-right"></span>
                </li>
                <li>
                    <a href="javascript:">Sales overview</a>
                </li>
                <li class="pull-right btn blue disabled mini">
                    <i class="fa fa-calendar"></i>
                    {{ "now"|date("d-F-Y") }}
                </li>
            </ul>
        </div>
    </div>
    <!-- END PAGE HEADER-->
    <!-- BEGIN PAGE CONTENT-->
    <div class="row-fluid">
        <div class="span12">
            <div class="portlet box green">
                <div class="portlet-title">
                    <div class="caption"><i class="icon-reorder"></i>Sales Overview</div>
                </div>
                <div class="portlet-body form">
                    <form class="search-form" action="" method="get" name="itemSearch">
                        <div class="row-fluid">
                            <div class="span6"></div>
                            <div class="span6">
                                <div class="controls">
                                    <div class="input-append span12">
                                        {%  set months = ['January','February','March','April','May','June','July','August','September','October','November','December'] %}
                                        <select class="span6 m-wrap" name="month" id="month">
                                            <option>--- Select Month---</option>
                                            {% for month in months %}
                                                <option {% if searchForm.month is defined and searchForm.month == month %}selected="selected"{% endif %} value="{{ month }}">{{ month }}</option>
                                            {% endfor %}
                                        </select>
                                        {%  set years = ['2018','2019','2020','2021','2022','2023','2024','2025'] %}
                                        <select class="span5 m-wrap" name="year" id="year">
                                            <option>--- Select Year---</option>
                                            {% for year in years %}
                                                <option {% if searchForm.year is defined and searchForm.year == year %}selected="selected"{% endif %} value="{{ year }}">{{ year }}</option>
                                            {% endfor %}
                                        </select>
                                        <div class="btn-group">
                                            <button class="btn blue" type="submit"><i class="icon-search"></i></button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                    <div class="form-actions portlet box green">
                        <div class="portlet-title">
                            <div class="caption"><i class="icon-money"></i>Sales Overview</div>
                            <div class="actions">
                                <a target="_blank" href="" class="btn purple"><i class="icon icon-print"></i> Print</a>
                            </div>
                        </div>
                    </div>
                    <div class="clearfix">&nbsp;</div>
                    <table class="table table-bordered table-striped table-condensed flip-content ">
                        <thead class="flip-content ">
                        <tr class="head-green">
                            <td>&nbsp;&nbsp;<i class="icon-list"></i></td>
                            <th class="numeric">Date</th>
                            <th class="numeric">Sub-Total</th>
                            <th class="numeric">Discount</th>
                            <th class="numeric">Net Total</th>
                            <th class="numeric">Receive</th>
                            <th class="numeric">Balance</th>
                        </tr>
                        </thead>
                        <tbody id="invoiceParticulars">
                        {% for entity in entities %}
                            <tr>
                                <td  class="numeric">{{ loop.index }}</td>
                                <td  class="numeric">{{ entity.date|date('d-m-Y') }}</td>
                                <td  class="numeric">{{ entity.subTotal|number_format }}</td>
                                <td  class="numeric">{{ entity.discount|number_format }}</td>
                                <td  class="numeric">{{ (entity.subTotal - entity.discount)|number_format }}</td>
                                <td  class="numeric">{{ entity.receive|number_format }}</td>
                                <td  class="numeric">{{ (entity.subTotal - entity.discount - entity.receive) |number_format }}</td>

                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                    <div class="clearfix">&nbsp;</div>
                </div>
            </div>
        </div>
    </div>
    {% endblock %}
    {% block cssBlock %}
        {% stylesheets filter='cssrewrite'
        '@AccountingBundle/Resources/public/css/style.css'
        %}
        <link rel="stylesheet" href="{{ asset_url }}"/>
        {% endstylesheets %}
    {% endblock  %}
    {% block javascriptBlock %}
        {{ parent() }}
        {% javascripts
        '@AccountingBundle/Resources/public/js/approveProcess.js'
        %}
        <script type="text/javascript" src="{{ asset_url }}"></script>
        {% endjavascripts %}
    {% endblock %}
    {% block documentready %}
        {{ parent() }}
        AccountingApproveProcess();
    {% endblock %}
