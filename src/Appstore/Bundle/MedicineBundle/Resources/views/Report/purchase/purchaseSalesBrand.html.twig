{% extends '::base.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <!-- BEGIN PAGE HEADER-->
        <div class="row-fluid">
            <div class="span12">
                <h3 class="page-title">
                    Brand base Purchase Report
                </h3>
                <ul class="breadcrumb">
                    <li>
                        <i class="icon-home"></i>
                        <a href="{{ path('homepage') }}">Home</a>
                        <span class="icon-angle-right"></span>
                    </li>
                    <li><a href="#">Brand base Purchase Report</a></li>
                    <li class="pull-right btn blue disabled mini">
                        <i class="fa fa-calendar"></i>
                        {{ "now"|date("d-F-Y") }}
                    </li>
                </ul>
            </div>
        </div>
        <!-- END PAGE HEADER-->
        <!-- BEGIN PAGE CONTENT-->
        <div class="row-fluid">
            <div class="span12">

                <div class="portlet box green tabbable">

                    <div class="portlet-title">
                        <div class="caption"><i class="icon-reorder"></i>Brand base Purchase Report</div>
                    </div>
                    <div class="portlet-body flip-scroll">
                        <div class="row-fluid">
                            <form class="search-form" action="" method="get" name="itemSearch">
                                <div class="row-fluid">
                                    <div class="span6">
                                        {% if option.isBranch == 1 %}
                                            <select name="branch" id="branch" class="span12 m-wrap">
                                                <option value=""> ---Select Branch --- </option>
                                                {% for bran in branches %}
                                                    <option {% if searchForm.branch is defined and searchForm.branch == bran.id %} selected="selected" {% endif %} value="{{ bran.id }}">{{ bran.name }}</option>
                                                {% endfor %}
                                            </select>
                                        {% endif %}
                                    </div>
                                    <div class="span3">
                                        <input type="text" id="startDate" name="startDate"  class="span12 m-wrap dateCalendar" value="{% if searchForm.startDate is defined %}{{ searchForm.startDate }}{% endif %}"  autocomplete="off" placeholder="{% if searchForm.startDate is defined %}{{ searchForm.startDate }}{% else %}Start date{% endif %} ">
                                    </div>
                                    <div class="span3">
                                        <div class="controls">
                                            <div class="input-append span10">
                                                <input type="text" id="endDate" name="endDate"  class="span12 m-wrap dateCalendar" value="{% if searchForm.endDate is defined %}{{ searchForm.endDate }}{% endif %}"  autocomplete="off" placeholder="{% if searchForm.endDate is defined %}{{ searchForm.endDate }}{% else %}End date{% endif %}  ">
                                                <div class="btn-group">
                                                    <button class="btn blue" type="submit"><i class="icon-search"></i></button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </form>
                        </div>
                        <table class="table table-bordered table-striped table-condensed flip-content ">
                            <thead class="flip-content ">
                            <tr class="head-blue">
                                <th class="numeric" >Brand Name</th>
                                <th class="numeric" >Mode</th>
                                <th class="numeric" >Purchase Price</th>
                                <th class="numeric" >Remaining Price</th>
                                <th class="numeric" >Sales Price</th>
                                <th class="numeric" >Sales Purchase Price</th>
                                <th class="numeric" >Profit/Loss</th>
                            </tr>
                            </thead>
                            <tbody id="sales">
                            {% set total=0 %}
                            {% set payment=0 %}
                            {% set due = 0 %}
                            {% for entity in brands %}
                                <tr>
                                    <td class="numeric" >{{ entity.brandName }}</td>
                                    <td class="numeric" ><strong>{{ entity.mode|capitalize }}</strong></td>
                                    <th class="numeric" >{{ purchasePrice[entity.brandName].purchasePrice|default|number_format  }}</th>
                                    <th class="numeric" >{{ purchasePrice[entity.brandName].remainingPurchasePrice|default|number_format  }}</th>
                                    <th class="numeric" >{{ salesPrice[entity.brandName].salesPrice|default|number_format  }}</th>
                                    <th class="numeric" >{{ salesPrice[entity.brandName].purchasePrice|default|number_format  }}</th>
                                    <th class="numeric" >{{ (salesPrice[entity.brandName].salesPrice|default(0) - salesPrice[entity.brandName].purchasePrice|default(0))|default|number_format  }}</th>
                                </tr>
                            {% endfor %}
                            </tbody>
                            <tfoot><tr><th>Grand</th><th>{{ total|number_format }}</th><th>{{ total|number_format }}</th><th>{{ total|number_format }}</th><th>{{ payment|number_format }}</th><th>{{ (total-payment)|number_format }}</th><th></th></tr></tfoot>
                        </table>
                    </div>
                </div>

            </div>
        </div>
    </div>
{% endblock %}
{% block javascriptBlock %}
    {{ parent() }}
    {% javascripts
    '@InventoryBundle/Resources/public/js/approveProcess.js' %}
    <script type="text/javascript" src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}
{% block documentready %}
    {{ parent() }}
    ApproveProcess();
{% endblock %}
