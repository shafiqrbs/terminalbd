<nav class="navbar navbar-inverse ">
    <section id="top-bar">
        <div class="container" id="menu">
            <div class="row">
                <div class="col-md-6 text-left">
                    <div class="box">
                        <ul class="info-links">
                            {% if globalOption.templateCustomize.showCalendar == 1 %}
                                <li><i class="fa fa-calendar"></i>&nbsp;{{ "now"|date("F j, Y") }}</li>{% endif %}
                            {% if globalOption.templateCustomize.showEmail == 1 and globalOption.email is not empty %}
                                <li><i class="fa fa-envelope"></i>&nbsp;{{ globalOption.email }}</li>{% endif %}
                            {% if globalOption.templateCustomize.showMobile == 1 %}
                                <li><i class="fa fa-mobile"></i>&nbsp;{{ globalOption.mobile }}</li>{% endif %}
                        </ul>
                    </div>
                </div>
                <div class=" col-md-6 text-right">
                    <div class="box">
                        <ul class="social-links">
                            {% if globalOption.facebookPageUrl == 1 %}
                                <li><a href="https://{{ globalOption.facebookPageUrl }}"><i class="fa fa-facebook"></i></a>
                                </li>{% endif %}
                            {% if globalOption.templateCustomize.showSearch == 1 %}
                                <li><a href="javascript:" id="searchEvent"><i class="fa fa-search"></i></a>
                                </li>{% endif %}
                            <li><a href="javascript:" class="register-btn"><i class="fa fa-sign-in"></i></a></li>
                            {% if  app.user %}
                                <li><a href="{{ path('fos_user_security_logout') }}" class=""><i
                                                class="fa fa-unlock-alt"></i></a></li>
                            {% else %}
                                <li><a href="javascript:" class="login-preview"><i class="fa fa-lock"></i></a></li>
                            {% endif %}
                            {% if globalOption.ecommerceConfig.cart == 1 %}
                                <li>
                                    <a href="{{ path('webservice_product_cart_details',{'subdomain':globalOption.subDomain}) }}"
                                       class=""><i class="fa fa-shopping-cart"></i></a></li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>
    {% if globalOption.featureWidgets is defined and globalOption.featureWidgets is not empty %}
        <section class="feature-widget-header-top" >
            {% render controller("FrontendBundle:EcommerceWidget:featureWidget",{'globalOption':globalOption.id,'pageName':pageName,'position':'header-top'}) %}
        </section>
    {% endif %}
    <style>

        .input-group-field {
            display: table-cell;
            vertical-align: middle;
            border-radius: 4px;
        }

        .input-group-lg > .form-control, .input-group-lg > .input-group-addon, .input-group-lg > .input-group-btn > .btn {
            border-radius: 0 !important;
        }

        .input-group-field .form-control, .input-group-field .form-control {
            border-radius: inherit !important;
        }

        .input-group-field:not(:first-child):not(:last-child) {
            border-radius: 0;
        }

        .input-group-field:not(:first-child):not(:last-child) .form-control {
            border-left-width: 0;
            border-right-width: 0;
        }

        .input-group-field:last-child {
            border-top-left-radius: 0;
            border-bottom-left-radius: 0;
        }

        ul.dropdown-cart {
            min-width: 250px;
        }

        ul.dropdown-cart li .item {
            display: block;
            padding: 3px 10px;
            margin: 3px 0;
        }

        ul.dropdown-cart li .item:hover {
            background-color: #f3f3f3;
        }

        ul.dropdown-cart li .item:after {
            visibility: hidden;
            display: block;
            font-size: 0;
            content: " ";
            clear: both;
            height: 0;
        }

        ul.dropdown-cart li .item-left {
            float: left;
        }

        ul.dropdown-cart li .item-left img,
        ul.dropdown-cart li .item-left span.item-info {
            float: left;
        }

        ul.dropdown-cart li .item-left span.item-info {
            margin-left: 10px;
        }

        ul.dropdown-cart li .item-left span.item-info span {
            display: block;
        }

        ul.dropdown-cart li .item-right {
            float: right;
        }

        ul.dropdown-cart li .item-right button {
            margin-top: 14px;
        }

        .search-box{
            border: 1px solid #000;
        }
        .btn-group-search{
            height: 46px;
            margin-left: 0;
            width: 50px;
        }
        .btn-cart{
             border:1px solid #000;
             height: 48px;
        }
        .input-group-lg{
            height: 46px;
            border: 0;
            border-left:1px solid #000 ;
        }

        .select2-container .select2-selection--single {
            box-sizing: border-box;
            cursor: pointer;
            display: block;
            height: 46px;
            user-select: none;
            -webkit-user-select: none;
        }

        .select2-container--default .select2-selection--single {
            background-color: #fff;
            border: 1px solid #fff;
            border-radius: 0 !important;
        }

        .select2-container .select2-selection--single .select2-selection__rendered {
            display: block;
            padding-left: 8px;
            padding-right: 20px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .select2-container--default .select2-selection--single .select2-selection__rendered {
            color: #444;
            line-height: 46px;
        }

        .select2-container--default .select2-selection--single .select2-selection__arrow {
            height: 46px;
            position: absolute;
            top: 1px;
            right: 1px;
            width: 20px;
        }

    </style>
    <section id="header">
        <div class="container">
            <div class="row navbar-header">
                <div class="col-md-3">
                    <a class="img-logo"
                       href="{{ path('ecommercesubdomain') }}">{% if globalOption.templateCustomize.webPath('logo') is not empty %}
                            <img src="{{ image(globalOption.templateCustomize.webPath('logo')).resize() }}"
                                 alt="company logo" />{% else %}{{ globalOption.name }}{% endif %}</a>
                </div>
                <div class="col-md-9">
                    {% if globalOption.templateCustomize.showSearch == 0 %}

                            <div class="row bs-example-form" id="header-form">
                                <div class="col-lg-10">
                                    <form class="bs-example " data-example-id="input-group-segmented-buttons">
                                        <div class='input-group search-box' >
                                            <div class='input-group-field'>
                                                {{ categoryTree|raw }}
                                            </div>
                                            <input type='text' class='form-control input-group-lg' style="width: 351px" name="product" id="product"  placeholder="Find Your Favorite Products"/>
                                            <span class="input-group-btn">
                                                <button class="btn btn-group btn-group-search" type="button"><span class="glyphicon glyphicon-search"></span></button>
                                            </span>
                                        </div>

                                    </form>
                                </div>
                                <div class="col-lg-2">
                                        <div class="input-group dropdown navbar-right">
                                            <div class="input-group-btn">
                                                <button type="button" class="btn btn-group btn-cart dropdown-toggle" data-toggle="dropdown"  aria-expanded="false"><span
                                                            class="glyphicon glyphicon-shopping-cart"></span></button>
                                                <a  style=" border:1px solid #000; border-left:none" href="#" class="btn dropdown-toggle" data-toggle="dropdown" role="button"
                                                   aria-expanded="false"> <span class="label-success badge">0</span>
                                                    &nbsp;<span class="label label-warning">৳ <em>0.00</em></span></a>
                                                <ul class="dropdown-menu dropdown-cart" role="menavbar-rightnu">
                                                    <li>
                  <span class="item">
                    <span class="item-left">
                        <img src="http://lorempixel.com/50/50/" alt=""/>
                        <span class="item-info">
                            <span>Item name</span>
                            <span>23$</span>
                        </span>
                    </span>
                    <span class="item-right">
                        <button class="btn btn-xs btn-danger pull-right">x</button>
                    </span>
                </span>
                                                    </li>
                                                    <li>
                  <span class="item">
                    <span class="item-left">
                        <img src="http://lorempixel.com/50/50/" alt=""/>
                        <span class="item-info">
                            <span>Item name</span>
                            <span>23$</span>
                        </span>
                    </span>
                    <span class="item-right">
                        <button class="btn btn-xs btn-danger pull-right">x</button>
                    </span>
                </span>
                                                    </li>
                                                    <li>
                  <span class="item">
                    <span class="item-left">
                        <img src="http://lorempixel.com/50/50/" alt=""/>
                        <span class="item-info">
                            <span>Item name</span>
                            <span>23$</span>
                        </span>
                    </span>
                    <span class="item-right">
                        <button class="btn btn-xs btn-danger pull-right">x</button>
                    </span>
                </span>
                                                    </li>
                                                    <li>
                  <span class="item">
                    <span class="item-left">
                        <img src="http://lorempixel.com/50/50/" alt=""/>
                        <span class="item-info">
                            <span>Item name</span>
                            <span>23$</span>
                        </span>
                    </span>
                    <span class="item-right">
                        <button class="btn btn-xs btn-danger pull-right">x</button>
                    </span>
                </span>
                                                    </li>
                                                    <li class="divider"></li>
                                                    <li><a class="text-center" href="">View Cart</a></li>
                                                </ul>
                                            </div><!-- /btn-group -->
                                        </div><!-- /input-group -->
                                </div>
                            </div>
                    {% endif %}
                </div>

            </div>
        </div>
    </section>
    <section id="menu-bar" class="sticky">
        <div class="container">
            <nav class="navbar navbar-mega-menu">
                <div class="navbar-header">
                    <button class="navbar-toggle" type="button" data-toggle="collapse"
                            data-target=".js-navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                </div>
                <div class="collapse navbar-collapse js-navbar-collapse">

                    <ul class="nav navbar-nav">
                        {% for  menu in globalOption.ecommerceMenus  %}
                        <li class="dropdown mega-dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">{{ menu.name }}<span
                                        class="caret"></span></a>
                            <ul class="dropdown-menu mega-dropdown-menu">
                                {% if menu.features is not empty or menu.promotions is not empty or menu.discounts is not empty or menu.tags is not empty %}
                                <li class="col-sm-3" data-match-height="menu-content" style="background-color: #f9f9f9; border-right:1px solid #d6d9dd">
                                    {% if menu.features is not empty %}
                                    <div class="col-sm-12" id="menCollection" class="carousel feature" data-ride="carousel">
                                        <div class="carousel-inner">
                                            <div class="container wow featureInRight" data-wow-delay="2s">
                                                    <div class="row">
                                                        {% for feature in  menu.features %}
                                                            {% if(feature.webPath) %}
                                                                {% if feature.targetTo == 'Promotion' %}
                                                                    {% set path = path('webservice_product_promotion',{'id':feature.id}) %}
                                                                {% elseif feature.targetTo == 'Tag' %}
                                                                    {% set path = path('webservice_product_tag',{'id':feature.tag.id}) %}
                                                                {% elseif feature.targetTo == 'Discount' %}
                                                                    {% set path = path('webservice_product_tag',{'id':feature.discount.id}) %}
                                                                {% elseif feature.targetTo == 'Brand' %}
                                                                    {% set path = path('webservice_product') %}
                                                                {% else %}
                                                                    {% set path = path('webservice_product') %}
                                                                {% endif %}
                                                                <div class="item {% if loop.index == 1 %} active {% endif %}">
                                                                    <a href="{{ path }}"><img src="{{ image(feature.webPath).resize(220,160) }}"  class="img-responsive" alt="product 1"></a>
                                                                    <h4><small>{{ feature.name }}</small></h4>
                                                                    <button href="{{ path }}" class="btn btn-default" type="button"><span class="glyphicon glyphicon-heart"></span>Show More</button>
                                                                </div><!-- End Item -->
                                                            {% endif %}
                                                        {% endfor %}
                                                    </div>
                                                </div>
                                        </div><!-- End Carousel Inner -->
                                        <!-- Controls -->
                                        <a class="left carousel-control" href="#menCollection" role="button"
                                           data-feature="prev">
                                                <span class="glyphicon glyphicon-chevron-left"
                                                      aria-hidden="true"></span>
                                            <span class="sr-only">Previous</span>
                                        </a>
                                        <a class="right carousel-control" href="#menCollection" role="button"
                                           data-feature="next">
                                                <span class="glyphicon glyphicon-chevron-right"
                                                      aria-hidden="true"></span>
                                            <span class="sr-only">Next</span>
                                        </a>
                                    </div><!-- /.carousel -->
                                    {% endif %}
                                    {% if menu.promotions is not empty %}
                                    <ul class="col-sm-12">
                                        <li class="dropdown-header">Promotion's</li>
                                        {% for promotion in  menu.promotions %}
                                        <li><a href="{{ path('webservice_product',{'promotion':promotion.id}) }}">{{ promotion.name }}</a></li>
                                        {% endfor %}
                                    </ul>
                                    {% endif %}
                                    {% if menu.discounts is not empty %}
                                        <ul class="col-sm-12">
                                            <li class="dropdown-header">Discount Offer's</li>
                                            {% for discount in  menu.discounts %}
                                                <li><a href="{{ path('webservice_product',{'discount':discount.id}) }}">{{ discount.name }}</a></li>
                                            {% endfor %}
                                        </ul>
                                    {% endif %}
                                    {% if menu.tags is not empty %}
                                        <ul class="col-sm-12">
                                            <li class="dropdown-header">Popular Tag's</li>
                                            {% for tag in  menu.tags %}
                                                <li><a href="{{ path('webservice_product',{'tag':tag.id}) }}">{{ tag.name }}</a></li>
                                            {% endfor %}
                                        </ul>
                                    {% endif %}
                                </li>
                                {% endif %}
                                {% if menu.categories is not empty %}

                                    {% if menu.features is empty and menu.promotions is empty and menu.discounts is empty and menu.tags is  empty and  menu.brands is empty %}
                                        {% set column   = "col-sm-3" %}
                                        {% set class    = "col-sm-12" %}
                                    {% elseif (menu.features is not empty and  menu.brands is empty)  or (menu.promotions is not empty and  menu.brands is empty ) or (menu.discounts is not empty and  menu.brands is empty ) or (menu.tags is not  empty and  menu.brands is empty )  %}
                                        {% set column   = "col-sm-4" %}
                                        {% set class    = "col-sm-9" %}
                                    {% elseif (menu.features is not empty  and  menu.brands is not empty ) or (menu.promotions is not empty  and  menu.brands is not empty ) or (menu.discounts is not empty  and  menu.brands is not empty ) or ( menu.tags is not empty  and  menu.brands is not empty) %}
                                        {% set column   = "col-sm-6" %}
                                        {% set class    = "col-sm-6" %}
                                    {% else %}
                                        {% set class    = "col-sm-6" %}
                                    {% endif %}

                                <li class="{{ class }}" data-match-height="menu-content" style="border-right:1px solid #d6d9dd">
                                    {% render controller("FrontendBundle:EcommerceWidget:returnMegaCategoryMenu",{globalOption:globalOption, categories: menu.categories,column:column}) %}
                                </li>
                                {% endif %}
                                {% if menu.brands is not empty %}
                                <li data-match-height="menu-content" class="{% if (menu.features is empty or menu.promotions is empty or menu.discounts is empty or menu.tags is  empty ) and  menu.categories is empty %} col-sm-12 {% elseif menu.categories is empty %} col-sm-9 {% else %} col-sm-3 {% endif %}">
                                    <ul class="col-sm-12">
                                        <li class="dropdown-header">Brand's</li>
                                        {% for brand in menu.brands %}
                                          <li class="{% if menu.categories is empty %} col-sm-3 {% endif %}" ><a href="{{ path('webservice_product_brand',{'id' : brand.id}) }}">{{ brand.name }}</a></li>
                                        {% endfor %}
                                    </ul>
                                </li>
                                {% endif %}
                            </ul>
                        </li>
                        {% endfor %}
                    </ul>

                </div><!-- /.nav-collapse -->
            </nav>
        </div>
    </section>
</nav>
<!-- BEGIN NAVIGATION -->

<!-- END NAVIGATION -->


