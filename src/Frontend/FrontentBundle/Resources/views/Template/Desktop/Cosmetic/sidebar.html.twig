<div class="row">
    {% if globalOption.featureWidgets is defined and globalOption.featureWidgets is not empty %}
        <section class="feature-widget-body-bottom" >
            {% render controller("FrontendBundle:EcommerceWidget:featureWidget",{'globalOption':globalOption.id,'menu':menu.id,'position':'sidebar-top'}) %}
        </section>
    {% endif %}
    <div class="clearfix"></div>
    {% render controller("FrontendBundle:EcommerceWidget:sidebarProductFilter",{'globalOption':globalOption.id,'searchForm':searchForm}) %}
    <div class="clearfix"></div>
    {% if globalOption.featureWidgets is defined and globalOption.featureWidgets is not empty %}
        <section class="feature-widget-body-bottom" >
            {% render controller("FrontendBundle:EcommerceWidget:featureWidget",{'globalOption':globalOption.id,'menu':menu.id,'position':'sidebar-bottom'}) %}
        </section>
    {% endif %}
</div>
<style>
    .ul-check-list-brand , .ul-check-list{
       list-style: none;
        padding-left:15px;
        margin-left:0;
    }
    .ul-check-list-brand li{
       list-style: none;
        float: left;
        width: 100%;
        height: 30px;

    }
    .ul-check-list li{
       list-style: none;
        float: left;
        width: 50%;
        height: 30px;

    }
    .ul-check-list-brand .checkbox , .ul-check-list .checkbox{
        height: 20px;
        float: left;

    }
    .ul-check-list-brand .label , .ul-check-list .label{
      display: block;
      font-size: 90%;
      color: #000000;
      float: left;
      line-height: 22px;
      font-weight:normal;
    }



</style>