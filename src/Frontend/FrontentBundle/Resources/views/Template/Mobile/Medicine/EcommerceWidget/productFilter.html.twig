<form class="bs-example" action="{{ path(globalOption.subDomain ~ '_webservice_product_filter') }}"  method="get" data-example-id="input-group-segmented-buttons">
<div class="row  margin-bottom">
    <div class="input-group col-xs-12 col-md-12">{{ categoryTree|raw }}</div>
    <div class="input-group">
        <span class="input-group-btn"><button type="button" class="btn btn-info btn-sorted"><span class="fa fa-sort-numeric-asc"></span></button></span>
        <input type="text" name="search" id="search" class="form-control" value="{{ searchForm['search'] is defined ? searchForm['search'] :'' }}" placeholder="Search for over 20,000 products">
        <span class="input-group-btn"><button type="submit" class="btn btn-success"><span class="glyphicon glyphicon-search"></span></button></span>
        <span class="input-group-btn"><button type="button" class="btn btn-warning" id="filter"><span class="fa fa-camera"></span></button></span>
    </div>
    <div id="showFilter" style="display:none;">

        {% set sortBy = app.request.get('sortBy') %}
        <select name="sortBy" id="sortBy" class="input-selector item-select col-xs-12 col-md-12"
                onchange='this.form.submit()'>
            <option {% if sortBy == '' %} selected="selected" {% endif %} value="">-- Sorting by
                --
            </option>
            <option {% if sortBy == 'product.name=?=ASC' %} selected="selected" {% endif %}
                    value="product.name=?=ASC">Name (A - Z)
            </option>
            <option {% if sortBy == 'product.name=?=DESC' %} selected="selected" {% endif %}
                    value="product.name=?=DESC">Name (Z - A)
            </option>
            <option {% if sortBy == 'brand.name=?=ASC' %} selected="selected" {% endif %}
                    value="brand.name=?=ASC">Brand (A - Z)
            </option>
            <option {% if sortBy == 'brand.name=?=DESC' %} selected="selected" {% endif %}
                    value="brand.name=?=DESC">Brand (Z - A)
            </option>
            <option {% if sortBy == 'product.salesPrice=?=ASC' %} selected="selected" {% endif %}
                    value="product.salesPrice=?=ASC">Price (Low &gt; High)
            </option>
            <option {% if sortBy == 'product.salesPrice=?=DESC' %} selected="selected" {% endif %}
                    value="product.salesPrice=?=DESC">Price (High &gt; Low)
            </option>
        </select>
    </div>
    <div class="clearfix"></div>
    <div id="productFilter">
        <div class=" panel-group" id="accordion" role="tablist" aria-multiselectable="true" style="margin-top: 1px;">    <div class="panel-heading">
                <div class="sidebar-footer" >
                    <button type="button"  class="btn btn-success col-xs-12 col-md-12">  Upload Prescription</button>
                </div>
            </div>
            <div class="row-fluid">
                <form name="stockItemForm" method="post" action="{{ path(globalOption.subDomain ~ '_webservice_stock_item_create') }}" id="stockItemForm" class="register" novalidate="novalidate">
                    <div>
                        <input class="select2StockMedicine form-control" autocomplete="off" name="itemName" id="itemName"  placeholder="Search your desire medicine">
                        <input type="hidden" name="stockId" id="stockId">
                        <ul id="searchResult"></ul>
                        <div class="input-group">
                            <div class="input-group-btn">
                                <button type="button" class="btn btn-success" aria-label="Bold">৳ </button>                                         </div>
                            <input type="text"  name="salesPrice" id="salesPrice" disabled="disabled" style="width: 80px" class="form-control text-center" >
                            <div class="input-group-btn">
                                <button type="button" class="btn gray-dark-bg danger-font btn-number-stock text-center" data-type="minus" data-field="itemQuantity">
                                    <span class="glyphicon glyphicon-minus"></span>
                                </button>
                            </div>
                            <input type="text" class="form-control text-center" value="1" min="1" max="1000"  id="itemQuantity" name="itemQuantity">
                            <div class="input-group-btn">
                                <button type="button" class="btn gray-dark-bg danger-font btn-number-stock" data-type="plus" data-field="itemQuantity">
                                    <span class="glyphicon glyphicon-plus"></span>
                                </button>
                                <button type="button" id="unit" class="btn btn-success">Unit</button> <button type="submit" class="btn btn-warning " id="stockSubmit" data-url="{{ path(globalOption.subDomain ~ '_webservice_stock_item_create') }}"><span class="glyphicon glyphicon-shopping-cart"></span> Add</button>
                            </div>
                        </div><!-- /.input-group -->
                    </div>
                </form>
                <div class="clearfix"></div>
                 <div id="stockCart">
                     <form class="" id="prescription" name="prescription" action="{{ path('order_checkout',{'shop':globalOption.uniqueCode}) }}" method="post" enctype="multipart/form-data">
                         <table class="table table-hover table-striped table-bordered table-list">
                             <thead>
                             <tr style="background-color:#269abc; color:#ffffff">
                                 <th>Product Name</th>
                                 <th class="text-center">MRP</th>
                                 <th>Qnt</th>
                                 <th class="text-left">Total</th>
                             </tr>
                             </thead>
                             <tbody>
                             {% for product in cart.contents %}

                                 <tr>
                                     <td>
                                         <div class="media">
                                             <div class="media-body">
                                                 <h5 class="media-heading">{{ product.name }}</h5>
                                                 {% if product.brand is not empty %}<h5 class="media-heading"> by {{ product.brand }}</h5>{% endif %}

                                             </div>
                                         </div>
                                     </td>
                                     <td class="text-center">
                                         <strong>{{ globalOption.ecommerceConfig.currency }}
                                             &nbsp;{{ product.price }}</strong></td>
                                     <td class="text-center">
                                         <form class="addCart">
                                             <div class="input-group">
                                 <span class="input-group-btn">
                                    <button type="button" class="btn gray-dark-bg btn-number-cart"
                                            data-type="minus" data-field="quantity" data-id="{{ product.rowid }}"
                                            data-url="{{ path(globalOption.subDomain ~ '_webservice_product_cart_update',{'cartid': product.rowid}) }}"
                                            data-text="{{ product.id }}" data-title="{{ product.price }}">
                                             <span class="glyphicon glyphicon-minus"></span>
                                    </button>
                                 </span>
                                                 <input type="text" name="quantity" id="quantity-{{ product.rowid }}"
                                                        class="form-control input-number" value="{{ product.quantity }}"
                                                        min="1" max="100" style="width: 75px!important;">
                                                 <span class="input-group-btn">
                                   <button type="button" class="btn gray-dark-bg btn-number-cart"
                                           data-type="plus" data-field="quantity"
                                           data-id="{{ product.rowid }}"
                                           data-url="{{ path(globalOption.subDomain ~ '_webservice_product_medicine_cart_update',{'cartid': product.rowid}) }}"
                                           data-text="{{ product.id }}" data-title="{{ product.price }}">
                                           <span class="glyphicon glyphicon-plus"></span>
                                   </button>
                                 </span>
                                                 {% if product.productUnit is not empty %}
                                                     <span class="input-group-btn">
                                             <button type="button"  class="btn btn-warning">{{ product.productUnit }}</button>
                                         </span>
                                                 {% endif %}
                                                 <span class="input-group-btn">
                                     <button id="{{ product.rowid }}"
                                             data-url="{{ path(globalOption.subDomain ~ '_webservice_product_cart_remove',{'cartid':product.rowid}) }}"
                                             class="btn btn-danger hunger-remove-cart">
                                         <span class="glyphicon glyphicon-trash"></span>
                                     </button>
                                 </span>

                                             </div>
                                         </form>
                                     </td>
                                     <td class="text-left">
                                         {{ globalOption.ecommerceConfig.currency }} <strong
                                                 id="btn-total-{{ product.rowid }}">{{ product.price * product.quantity }}</strong>
                                     </td>

                                 </tr>
                             {% endfor %}
                             <tr>
                                 <td colspan="3" class="text-right"><strong>Subtotal</strong></td>
                                 <td class="text-left col-sm-1 col-md-1"><strong>{{ globalOption.ecommerceConfig.currency }} <span class="cartTotal">{{ cart.total }}</span></strong></td>
                             </tr>
                             <tr>
                                 <td colspan="3" class="text-right"><h5><strong>Coupon Code</strong></h5></td>
                                 <td class="text-left col-sm-1 col-md-1"><input type="text"
                                                                                style="width: 80px;
    border-radius: 0;
    border: 1px solid #000;
    font-size: 11px;
    padding: 3px;"
                                                                                class=""
                                                                                name="couponCode"
                                                                                id="couponCode"></td>
                             </tr>
                             <tr>
                                 <td colspan="3" class="text-right"><strong>Grand Total</strong></td>
                                 <td class="col-sm-1 col-md-1 text-left">
                                     <strong>{{ globalOption.ecommerceConfig.currency }} <span class="cartTotal">{{ cart.total }}</span></strong>
                                 </td>
                             </tr>
                             <tr>
                                 <td colspan="4" class="text-right">
                                     <input type="file" id="prescriptionFile" name="prescriptionFile" class="m-wrap span12 form-control" placeholder="">
                                 </td>
                             </tr>
                             <tr>
                                 <td colspan="4">
                                     <ul>
                                         <li>File must be JPG, GIF, PDF or PNG, less than 1MB</li>
                                         <li>Please file attach afetr login, dont forget it.</li>
                                     </ul>
                                 </td>

                             </tbody>
                         </table>
                     </form>
                 </div>
                <div class="clearfix"></div>
            </div>
            <div class="panel-heading">
                <div class="sidebar-footer" >
                    {% if  app.user %}
                    {% if cart.total > 0 %}
                    <button type="submit"  class="btn btn-primary col-xs-12 col-md-12" id="filter"><span class="glyphicon glyphicon-cart"></span> Take Order <span class="glyphicon glyphicon-play"></span></button>
                        {% endif %}
                    {% else %}
                    <button type="submit"  class="btn btn-primary col-xs-12 col-md-12" id="filter"><span class="glyphicon glyphicon-cart"></span>  Login & Take Order <span class="glyphicon glyphicon-play"></span></button>
                    {% endif %}
                </div>
            </div>
        </div><!-- panel-group -->
    </div>
</div>
</form>
<style>
    #accordion .panel-title a{
        font-weight: normal !important;
    }
    .input-group{
        margin-bottom: 0!important;
    }
    #priceStart , #priceEnd{
        height:30px!important;
        border-radius:0;
        border: 1px solid #d2d2d2;

    }
    #accordion .panel-body{
        background: #FFFFff!important;
    }
    #productFilter{
        display:none;
    }
</style>