{% extends '::base.html.twig' %}

{% block body -%}
    <h1>HomePage list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Content</th>
                <th>Slidertype</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('homepage_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.content }}</td>
                <td>{{ entity.sliderType }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('homepage_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('homepage_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('homepage_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
